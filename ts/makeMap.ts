function makeMap(rooms){
    let num2;
    let positionedRooms = [];
    for(let i = 0; i < 10; i = num2){
        for(const room of rooms) {
            //
        }
        positionedRooms.push(rooms[0]);
        let num = 1;
        while (num < rooms.length){

        }
    }
}